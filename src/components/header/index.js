import { Fragment } from "react";

export const Header = () => {
    return(
        <Fragment>
            <header className="_header">
                header
            </header>
        </Fragment>
    );
};