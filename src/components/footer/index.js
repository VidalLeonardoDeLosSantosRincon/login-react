import { Fragment } from "react";

export const Footer = () => {
    return(
        <Fragment>
            <footer className="_footer">
                footer
            </footer>
        </Fragment>
    );
};